#:kivy 1.11.1
<MyWidget>:
    Button:
        id: btn
        text: 'Incrementar'
        background_color: [1,0,0,1]
        size_hint: (0.2,0.2) # 20% de altura e 20% de lagura do widget base / size_hint:(None, None) para desconsiderar a dica e considerar de forma absoluta o valor que quer e bota size: (80, 80) -> 80 pixels por 80 pixels
        pos_hint: {'center_x':0.5,'center_y':0.5} # ou {‘x’: 0.3, ‘y’: 0.3} / Se colocar objeto fora da tela não da erro, pode colocar right: 2 e top:1, só n vai aparecer na tela
        

# BoxLayoult só da 1 grau de liberdade